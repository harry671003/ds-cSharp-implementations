{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/binary-search-tree/tests/tests.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}